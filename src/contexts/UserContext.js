import { createContext } from "react";

// Context API - Hook
// Criar contexto - createContext()
// Recebe como parametro opcional o -  estado inicial
export const UserContext = createContext();

// Logica do contexto